import * as Constants from '../const';

describe('constants', () => {
  it('should be defined', () => {
    expect(Constants.RNS_SHOW_NOTIFICATION).toBeDefined();
    expect(Constants.RNS_HIDE_NOTIFICATION).toBeDefined();
  });
});
